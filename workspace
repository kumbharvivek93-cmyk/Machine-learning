the road map of machine learning is as the folling ::

intro to the machine learning 
sciket learn
  into
  loading /fecting the dataset in the program 
  spliting the data 
  scaling the data
  preprocassing
  trainig the model
===============================================================================================================================================================

models performance and accuracy sore or R2 socre depends upon the data / outliers/ relation ship between target and data

the relation bet. feture and data is found with

print(df.corr(numeric_only=True))



===============================================================================================================================================================

for the machine learning first step is to celan data
then all intermidiate process and  -> train a model 

final step is to make a pipeline and integrate this model with flasks web app


i love machine learning .  i love it so much and more and more ... perticularly the regression one i have done linear reg, multiple liner reg , popynomial reg, now next is redge , laso
and elastic regression
