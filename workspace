the road map of machine learning is as the folling ::

intro to the machine learning 
sciket learn
  into
  loading /fecting the dataset in the program 
  spliting the data 
  scaling the data
  preprocassing
  trainig the model


models performance and accuracy sore or R2 socre depends upon the data / outliers/ relation ship between target and data

the relation bet. feture and data is found with

print(df.corr(numeric_only=True))
